name: plone
maintainer: [Adam Stokes <adam.stokes@ubuntu.com>]
summary: Plone deployment
description: |
  Plone application managed by Juju
provides:
  website:
    interface: http
  wsgi:
    interface: wsgi
    scope: container
requires:
  db:
    interface: zodb
